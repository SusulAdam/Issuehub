import {ViewStyle} from 'react-native';

type HomeStyles = {
  container: ViewStyle;
};

export const homeStyles: HomeStyles = {
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    flex: 1,
    marginTop: 10,
  },
};
